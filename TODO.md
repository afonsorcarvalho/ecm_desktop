# TODO — ecm_desktop

## Em curso
- (nenhum)

## Pendente
- **Upload de pasta completa** — recursivo, replicando árvore. Sinais:
  - `<input type="file" webkitdirectory multiple>` no [UploadDropzone.tsx](renderer/components/UploadDropzone.tsx) — browser entrega `File[]` com `webkitRelativePath` (ex: `Contratos/2026/CTR_X.pdf`).
  - Parsear `webkitRelativePath` → criar `dms.directory` ausentes recursivamente (`ecmApi.createDirectory` já existe), depois `uploadFile` em cada arquivo apontando pra dir criado.
  - UI: barra de progresso "criando pastas (X/Y)" → "enviando arquivos (M/N)". Reusar [UploadQueueBar.tsx](renderer/components/UploadQueueBar.tsx).
  - Edge case: pastas vazias na origem → criar dir vazio? sim, pra preservar estrutura.
  - DnD opcional (drop folder no dropzone): `dataTransfer.items` com `webkitGetAsEntry()` recursivo — só funciona em build Windows nativo (WSLg drop não transfere bytes; ver gotcha #7 [CLAUDE.md](CLAUDE.md)).
- **Preview Word/Excel no FilePreviewModal** — paralelo ao [PdfRenderer.tsx](renderer/components/PdfRenderer.tsx). Opções a avaliar:
  - **Render server-side → PDF**: chamar `unoconv` / `libreoffice --headless` no Odoo, retornar PDF temporário, reusar `PdfRenderer`. Pesado mas alta fidelidade.
  - **Client-side**: `docx-preview` (Word HTML), `xlsx` + `react-data-grid` ou `luckyexcel`/`x-data-spreadsheet` (Excel). Sem dep servidor; fidelidade média.
  - **Google Docs Viewer / Office Online iframe**: rápido mas exige URL pública (não combina com docs confidenciais ECM).
  - Decisão depende de: aceita dep server (LibreOffice) ou prefere pure JS no renderer. Tem WSL/Docker — server-side viável.
  - Integrar em [FilePreviewModal.tsx](renderer/components/FilePreviewModal.tsx) detectando mimetype `application/vnd.*officedocument.*` ou extensão `.doc/.docx/.xls/.xlsx`.
- **Watch folder múltiplas pastas** — hoje [useWatchFolder.ts](renderer/hooks/useWatchFolder.ts) + tela [/settings](renderer/app/settings) suportam só 1 pasta local sincronizada. Permitir N pastas, cada uma mapeada pra um `dms.directory` destino diferente. Sinais:
  - Persistência: trocar config single → array `[{localPath, directoryId, enabled}]` (electron-store ou settings atual).
  - Hook: `useWatchFolder` vira `useWatchFolders` — registrar N watchers (Electron `fs.watch` / chokidar no main process via IPC).
  - UI /settings: lista editável (add/remove linha), picker de pasta local + dropdown pasta ECM destino por linha.
  - Edge: mesma pasta local 2× → bloquear; pasta dentro de outra observada → avisar.
- **Fase 5** — admin IA criar nova pasta (wizard `CreateDirectoryFromAi.tsx`), undo de apply (10s window).
- **Fase 6** — testes Python pytest pra `afr.ecm.ai.classifier` + fixtures demo.

## Feito
- 2026-05-19 — Extração texto Word/Excel (H): `services/office_text_engine.py` (`.docx` via python-docx — parágrafos+tabelas; `.xlsx` via openpyxl — células de todas as abas). Integrado no `ocr_engine.is_supported_mimetype` + `extract()` dispatch → docx/xlsx passam por TODO o pipeline OCR existente (ocr_state, ocr_text, index_content, hook keywords, hook IA) sem código novo. `.doc`/`.xls` legados retornam skip claro. ClassifyWizard checkbox OCR habilitado pra `.docx/.xlsx`. requirements.txt += python-docx, openpyxl. Testado: docx→texto, xlsx→"aba | célula | célula".
- 2026-05-19 — INFRA libs NLP: `requirements.txt` += `numpy<2`, `yake`, `spacy>=3.7,<3.8`; Dockerfile += `python -m spacy download pt_core_news_sm` + verificação. Recuperados `scripts/odoo_venv_wrapper.sh` e `scripts/patch_website_request.py` (sumiram do repo — Dockerfile faz COPY deles, rebuild quebraria). Rebuild ainda não rodado (container atual já tem libs via pip manual); rodar `docker compose build web` quando der pra validar.
- 2026-05-19 — Keywords (K): pipeline híbrida pós-OCR. `services/keyword_engine.py` (YAKE keywords + spaCy `pt_core_news_sm` entidades, lazy/degradação graciosa). Campos `dms.file.keywords` + `entities`, populados inline em `_ocr_process`, jogados no `index_content` pra busca FTS. Keywords/entidades locais entram no prompt Groq (`gather_signals`/`build_prompt`); Groq devolve `keywords` refinadas no mesmo JSON (zero chamada extra) → salvas em `afr.ecm.ai.suggestion.keywords`, aplicadas ao doc no accept. Decisão: spaCy `sm` (leve), opção (c) busca+tags, fundido na chamada Groq existente. Testado: OS0823 → keywords "AUTOCLAVE ETO, Steriliza Corporation" + classificou "Relatórios/Operacional" 0.9.
- 2026-05-19 — Sugestão IA na aba lateral direita: novo [AiSuggestionSummary.tsx](renderer/components/AiSuggestionSummary.tsx) compacto no painel `aside` após ShareButton. Estados: sem classificação → botão "Classificar IA"; pending → pasta+confiança+"Aceitar"; processing → spinner; failed → erro; aceita/rejeitada → "Reclassificar". Link "detalhes" abre preview na tab IA.
- 2026-05-19 — Fase 4 completa IA: badge `AiSuggestionBadge` wired no card da lista ([page.tsx](renderer/app/page.tsx)) — click abre preview na tab IA via prop `initialTab` em [FilePreviewModal.tsx](renderer/components/FilePreviewModal.tsx); polling React Query estendido pra disparar 5s enquanto `ai_state` pending/processing; bulk "✨ Classificar IA" (botão violeta) em [BulkActionBar.tsx](renderer/components/BulkActionBar.tsx) chamando `ecmApi.aiClassifyNow(ids)`.
- 2026-05-19 — Upload em lote: capacidade já existia (`input.multiple=true`, `useUploadQueue` 4 parallel). Adicionado: tooltip "Ctrl/Shift no picker" no botão Upload header, hint "vários ao mesmo tempo" no [UploadDropzone.tsx](renderer/components/UploadDropzone.tsx) inline, toast de batch concluído ("N enviados • M falhas") em [page.tsx](renderer/app/page.tsx) via tracking `prevActiveRef`.
- 2026-05-19 — Checkbox OCR no upload: [ClassifyWizard.tsx](renderer/components/ClassifyWizard.tsx) ganhou per-item checkbox + toggle "OCR em todos" com contador "X/Y suportado(s)". Default ON pra PDF/imagens (mime/ext). Pipeline propaga `ocrEnabled` via [useUploadQueue.ts](renderer/hooks/useUploadQueue.ts) → [`ecmApi.uploadFile`](renderer/lib/ecm-api.ts) → `dms.file.create({ocr_enabled})`.
- 2026-05-19 — Busca unaccent + case-insensitive: causa raiz era `unaccent` extension Postgres ausente nas DBs apesar de `unaccent=True` em [odoo.conf](../docker-compose.yml). `CREATE EXTENSION unaccent` em todas DBs (odoo_engenapp, odoo_ecm_test, odoo_ecm_desktop_test, etc). Zero mudança de código no renderer/afr_ecm. Validado: "cotacao" acha "Cotação", "RENOMEACAO" (upper+sem acento) acha "renomeação".
- 2026-05-19 — Fase 3 IA: hook auto-dispatch no fim de `_ocr_process` ([dms_file.py:890](../addons/afr_ecm/models/dms_file.py#L890)) chama `rec._ai_dispatch_classification()` quando `ocr_state='done'`. Pre-checks (ai_allow_external, ai_excluded, debounce 24h) dentro do dispatch. Testado end-to-end: arquivos 5 e 14 classificados automaticamente após OCR via queue_job channel `root.afr_ecm.ai_classify`.
- 2026-05-19 — Renomear arquivo: [`RenameFileModal.tsx`](renderer/components/RenameFileModal.tsx) (clone do folder modal), atalho F2 prioriza arquivo selecionado sobre pasta atual em [page.tsx:135](renderer/app/page.tsx#L135), usa `ecmApi.updateFile(id, {name})`. Input pré-seleciona base (sem extensão).
- 2026-05-19 — Fase 1 backend IA: models `afr.ecm.ai.suggestion` + `.alt`, extensões `dms.file` (ai_state, current_suggestion_id, last_classified_at), `dms.directory` (ai_excluded), security group `group_ai_classifier_admin` + ACL + record rules, settings via `ir.config_parameter`, views form/list, menu **ECM → Auditoria → Sugestões IA**. Module version `16.0.2.1.0`. Upgrade verificado em DB `odoo_ecm_desktop_test`.
- 2026-05-19 — Fase 2 service IA: `afr.ecm.ai.classifier` (AbstractModel) com `gather_signals`/`gather_taxonomy`/`build_prompt`/`call_groq`/`parse_and_validate`/`classify`, BM25 inline, `services/groq_client.py` (retry 4× backoff 5/15/30/60s, respeita `Retry-After`), RPCs em `dms.file` (`ai_classify_now`, `ai_apply_suggestion`, `ai_reject_suggestion`, `ai_ignore_suggestion`, `ai_list_suggestions`), dispatch via `with_delay(channel="root.afr_ecm.ai_classify")`. Teste shell real Groq passou.
- 2026-05-19 — Fase 4 mini UI: types `EcmAiSuggestion`/`EcmAiSuggestionAlt`/`AiSuggestionOverrides` + 5 métodos em `ecmApi`, componentes `AiSuggestionBadge.tsx` (canto card) e `AiSuggestionPane.tsx` (tab embedded), integrado em [FilePreviewModal.tsx](renderer/components/FilePreviewModal.tsx) como nova tab "✨ IA" (3 tabs: Sumário, OCR, IA pra PDF; 2 tabs OCR+IA pra não-PDF).
- 2026-05-19 — Seed árvore demo: 44 dirs em DB de teste (Contratos, Procedimentos, Relatórios, RH, Financeiro, Jurídico, Comercial, Qualidade, Administrativo, TI, Compras) todos com `description` HTML (manual) pra IA usar como contexto. IA reclassificou `alvara2026.pdf` corretamente: antes (3 dirs) → "Contratos/2026" 0.8 ; depois (44 dirs com manuais) → "Jurídico/Alvarás e Licenças" 0.9.
- 2026-05-19 — Fix bug crítico rate-limit (429): `classify()` não descarta mais sugestão anterior se Groq falhar. Errors transientes (429/5xx) lançam `UserError` preservando `ai_state` original; só erros estruturais criam registro `failed`. Painel ordena `pending > accepted > rejected > ignored > failed`. Banner amarelo quando última reclassificação falhou mas pending anterior existe. `ai_max_input_tokens` 8000→3500, `gather_taxonomy` trim agressivo (top 15-20 dirs + drop manuais bottom).
- 2026-05-19 — Cosmético cards: selecionado ganhou `ring-2 ring-accent`, `bg-accent/10`, `shadow-lg shadow-accent/30`, escala 1.02, badge `✓` no canto, nome em negrito accent.
- 2026-05-19 — Toast de feedback: `toast.promise` em aceitar/rejeitar/classificar com mensagens contextuais ("Remanejando \"X\"…" → "\"X\" movido para \"Y\" • tipo: Z • N tags").
